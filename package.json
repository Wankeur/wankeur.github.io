{
  "name": "portfolio-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-projects": "node generate-project-pages.js",
    "update": "npm run generate-projects && npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}